import Foundation

/// Updated by hand alongside each commit — not build-time-generated, so it
/// only reflects what was true when this file was last edited, not
/// necessarily every subsequent local change. Exists so "is this actually
/// the build with the latest fixes" is a glance at Account -> Help & FAQ
/// instead of cross-referencing session logs and database evidence.
enum BuildInfo {
    static let gitSHA = "837f8d5"
    static let builtAt = "2026-09-03"
}
